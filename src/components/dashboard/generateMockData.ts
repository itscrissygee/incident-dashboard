// generateMockData.ts
import { IncidentData, AnalystData } from "./types";

export const generateMockData = (timeFrame = "day") => {
  // Previous mock data generation code here
  // [I can provide the full code for this file if you'd like]
};
